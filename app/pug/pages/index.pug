extends ../base.pug

block content
    .swiper-container.sections.content-main__sections
        .swiper-wrapper
            for section in sections
                div(data-name=section.name, class="section section-"+section.name+" swiper-slide")
                    +dynamicPath(section.name, section)